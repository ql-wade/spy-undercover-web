# 谁是卧底 Web 版 — 产品需求文档（PRD）

> 项目：spy-undercover | 版本：v1.0 | 状态：待审批 | 日期：2026-04-25
> 基于：市场调研报告 v2.0 | 下一步：UI 原型设计

---

## 一、产品概述

### 1.1 产品定义

**"谁是卧底"Web 版发牌助手** 是一款面向线下聚会场景的同屏面对面 Web 应用，核心价值是**替代人工发牌环节**，让聚会游戏更流畅、更有趣。

**产品定位**：线下聚会辅助工具（非在线游戏平台）

**核心差异化**：
1. 🔄 **词库自动更新** — CF Workers 每日爬取，永不过时
2. 📱 **同屏面对面** — 一台设备多人传递，真实社交体验
3. 🎭 **双模式** — 经典 + 白板模式，覆盖新手到老玩家

### 1.2 产品目标

| 目标维度 | 具体指标 | 时间节点 |
|----------|----------|----------|
| 核心目标 | 周活跃局数 500 局/周 → 20000 局/周 | M1 → M6 |
| 用户体验 | 首屏加载 < 1.5s，翻牌动画 60fps | MVP |
| 词库规模 | 内置 300+ 词对 → 在线无限扩展 | v1.0 → v1.1 |
| 覆盖设备 | iOS Safari / Android Chrome / 微信内置浏览器 | MVP |

### 1.3 目标用户

| 用户画像 | 占比 | 核心场景 | 关键需求 |
|----------|------|----------|----------|
| 年轻白领（22-35岁） | 45% | 周末聚餐、KTV等位、公司团建 | 快速启动、词库丰富、无广告 |
| 大学生（18-24岁） | 30% | 宿舍夜聊、社团破冰、班会活动 | 无广告、白板模式、免费 |
| 家庭用户（30-50岁） | 15% | 春节/中秋家庭聚会、亲子互动 | 操作简单、词汇生活化 |
| 活动组织者（HR/主持人） | 10% | 公司团建、婚礼暖场、年会互动 | 投屏适配、快速上手 |

### 1.4 产品边界（明确不做）

| 不做 | 原因 |
|------|------|
| 用户注册/登录 | 聚会场景无需持久身份 |
| 在线语音/视频 | 不是在线游戏平台 |
| 排行榜/社交功能 | 增加复杂度，偏离核心场景 |
| 广告 | 伤害聚会体验 |
| 房间码多人联网 | MVP 不需要，v2.0 可选 |

---

## 二、核心功能需求

### 2.1 功能优先级总览

| ID | 功能模块 | 优先级 | 所属版本 | 估时 |
|----|----------|--------|----------|------|
| F1 | 发牌核心引擎 | P0 | v1.0 (MVP) | 3d |
| F2 | 经典游戏模式 | P0 | v1.0 (MVP) | 2d |
| F3 | 白板游戏模式 | P0 | v1.0 (MVP) | 1d |
| F4 | 内置词库（300+） | P0 | v1.0 (MVP) | 1d |
| F5 | 翻牌动画（3D翻转） | P1 | v1.0 (MVP) | 2d |
| F6 | 响应式布局 | P1 | v1.0 (MVP) | 2d |
| F7 | 游戏设置面板 | P1 | v1.0 (MVP) | 1d |
| F8 | 首次使用引导 | P2 | v1.0 (MVP) | 0.5d |
| F9 | 在线词库 API | P0 | v1.1 | 3d |
| F10 | 自定义词库 | P1 | v1.1 | 2d |
| F11 | PWA 离线支持 | P1 | v1.1 | 2d |
| F12 | 暗色模式 | P2 | v1.1 | 1d |
| F13 | 游戏记录 | P2 | v1.1 | 1d |
| F14 | 分享功能 | P2 | v1.1 | 1d |
| F15 | AI 生成词对 | P1 | v2.0 | 5d |
| F16 | 社区词库 | P1 | v2.0 | 5d |
| F17 | 统计面板 | P2 | v2.0 | 3d |
| F18 | 房间码模式 | P3 | v2.0 | 5d |

---

### 2.2 F1：发牌核心引擎（P0）

**功能描述**：游戏的核心发牌逻辑，负责角色分配和词语分发。

**输入参数**：
- 总人数 N（4-12 人）
- 卧底数量 S（1 或 2 个）
- 白板数量 B（0-2 个，白板模式下可选）
- 选定词对（wordA / wordB）

**输出结果**：
- N 张卡片，每张卡片包含：玩家编号、角色标签、词语内容（白板为空）

**分配算法**：

```
输入: N=7, S=2, B=1, wordA="苹果", wordB="梨子"

输出:
  Player 1: 平民 🍎 "苹果"
  Player 2: 平民 🍎 "苹果"
  Player 3: 平民 🍎 "苹果"
  Player 4: 卧底 🕵️ "梨子"
  Player 5: 卧底 🕵️ "梨子"
  Player 6: 白板 📋 [隐藏]
  Player 7: 平民 🍎 "苹果"
```

**规则约束**：
- 平民数 ≥ 卧底数 + 白板数（保证平民占优）
- 最少 4 人（3平民 + 1卧底）
- 最多 12 人
- 当 N ≤ 5 时，最多 1 个卧底
- 当 N ≥ 6 时，可选 1-2 个卧底
- 白板数量上限 = floor((N - S - 1) / 2)

**交互流程**：
1. 用户在设置页配置参数
2. 点击"开始发牌"按钮
3. 屏幕显示 N 张背面朝上的卡片
4. 点击任意卡片 → 3D 翻转动画 → 显示词语
5. 当前查看者确认后点击"隐藏"→ 卡片翻回背面
6. 传递给下一位玩家

**验收标准**：
- [ ] 输入 4-12 人均能正确分配角色
- [ ] 角色比例符合规则约束
- [ ] 每次发牌结果随机且均匀分布
- [ ] 卡片翻转动画流畅（60fps）
- [ ] 词语显示后可重新隐藏

---

### 2.3 F2：经典游戏模式（P0）

**功能描述**：标准"谁是卧底"规则。

**角色定义**：
| 角色 | 数量 | 词语 | 胜利条件 |
|------|------|------|----------|
| 平民 | N - S - B | 词A（相同） | 投票淘汰所有卧底 |
| 卧底 | S | 词B（与A相似） | 存活到最后或淘汰所有平民 |
| 白板 | B | 无 | 混到最后存活（白板模式下启用） |

**游戏流程**：
```
设置参数 → 发牌 → 依次看词 → 轮流描述(每人30s) → 投票 → 揭晓结果
```

**描述阶段规则**：
- 按编号顺序轮流描述自己的词语
- 不能直接说出词语本身
- 每人描述 1 句（可自定义 1-3 句）
- 白板玩家需编造一个合理描述

**投票阶段规则**：
- 所有人同时指向怀疑的对象
- 得票最多者被淘汰（平局时重新投票或随机淘汰一人）
- 淘汰后揭晓其身份和词语
- 若卧底全部淘汰 → 平民胜利；若平民≤卧底 → 卧底胜利

**验收标准**：
- [ ] 经典模式流程完整可运行
- [ ] 支持自定义描述轮次（1-3轮）
- [ ] 投票淘汰逻辑正确
- [ ] 胜负判定准确
- [ ] 结果展示包含所有玩家身份和词语

---

### 2.4 F3：白板游戏模式（P0）

**功能描述**：在经典模式基础上增加"白板"角色，提升策略深度。

**与经典模式的区别**：
- 默认至少 1 个白板角色
- 白板玩家看不到任何词语，需通过听他人描述来伪装
- 白板的胜利条件：存活到最后（不区分阵营）
- 白板被淘汰时，不揭晓词语（因为本来就没有）

**白板模式特有交互**：
- 设置页默认勾选"启用白板"
- 白板卡片翻开时显示"📋 白板"而非词语
- 结果展示中白板玩家标记为特殊图标

**策略提示**（可选展示给用户）：
- 白板玩家应选择模糊描述，避免过于具体或过于空泛
- 平民应注意找出"描述总是很奇怪"的人
- 卧底可以利用白板的存在来混淆视听

**验收标准**：
- [ ] 白板模式可独立选择
- [ ] 白板角色分配正确
- [ ] 白板卡片显示"白板"标记
- [ ] 白板胜利条件判定正确
- [ ] 白板被淘汰时不泄露词语信息

---

### 2.5 F4：内置词库（P0）

**功能描述**：应用内置 300+ 词对，按难度分类。

**词库结构**：
```json
{
  "version": "1.0.0",
  "category": "builtin",
  "words": [
    {
      "id": "w001",
      "wordA": "苹果",
      "wordB": "梨子",
      "difficulty": "easy",
      "tags": ["水果", "食物", "生活"],
      "usageCount": 0
    }
  ],
  "meta": {
    "total": 320,
    "difficultyDistribution": { "easy": 120, "medium": 120, "hard": 80 }
  }
}
```

**难度分级标准**：

| 难度 | 词对数量 | 特征 | 适用场景 |
|------|----------|------|----------|
| 简单（Easy） | ~120 | 高频日常词汇，差异明显 | 新手教学、家庭聚会 |
| 中等（Medium） | ~120 | 有一定区分度，需要思考 | 朋友聚餐、常规游戏 |
| 困难（Hard） | ~80 | 语义接近、易混淆 | 老玩家挑战、高难度局 |

**词库管理机制**：
- 本地 localStorage 缓存已使用的词对
- 同一游戏 session 内不重复词对（除非手动选择）
- 提供"随机"和"指定难度"两种选择方式

**验收标准**：
- [ ] 内置 ≥ 300 词对
- [ ] 按 easy/medium/hard 三级分类
- [ ] 每个词对有 A/B 两个相似词
- [ ] 可按难度筛选
- [ ] 同一局内不重复（跨局可重复）

---

### 2.6 F5：翻牌动画（P1）

**功能描述**：3D 翻牌效果，防止旁人偷看词语。

**技术实现**：Framer Motion + CSS 3D Transform

**动画规格**：
| 参数 | 值 | 说明 |
|------|-----|------|
| 翻转轴 | Y 轴 | 从左到右翻转 |
| 时长 | 600ms | 含缓动曲线 |
| 缓动 | easeInOut | 自然减速 |
| 背面图案 | 统一卡背设计 | 带"?"问号装饰 |
| 正面样式 | 大字居中显示词语 | 字号自适应屏幕宽度 |
| 点击热区 | 整张卡片 | 移动端友好 |

**降级方案**：
- 低端设备自动降级为淡入淡出（检测 `prefers-reduced-motion`）
- 动画不影响核心功能——即使动画异常，词语仍可正常显示/隐藏

**验收标准**：
- [ ] 3D 翻转视觉效果流畅
- [ ] 翻转过程无法看到正反面内容（中间帧完全遮盖）
- [ ] 支持 `prefers-reduced-motion` 降级
- [ ] 卡片尺寸在不同屏幕上自适应

---

### 2.7 F6：响应式布局（P1）

**功能描述**：适配多种设备和屏幕方向。

**断点设计**：

| 断点 | 宽度范围 | 布局策略 | 主设备 |
|------|----------|----------|--------|
| xs | 320-428px | 单列竖排，大触控区域 | 手机竖屏 |
| sm | 429-640px | 单列宽松布局 | 手机横屏/大屏手机 |
| md | 641-768px | 双列网格 | 平板竖屏/小平板 |
| lg | 769-1024px | 三列网格 | 平板横屏/小笔记本 |
| xl | 1025px+ | 四列网格 + 侧边栏 | 桌面/投屏 |

**核心原则**：
- **移动端优先**（90%+ 使用场景在手机）
- **横屏优化**（聚会传递场景常为手持横屏）
- **触控友好**（最小触控目标 44x44px）
- **大字体**（词语字号 ≥ 24px，确保远距离可见）

**投屏模式**（xl 断点特有）：
- 更大的卡片和字体
- 高对比度配色
- 简化界面元素（隐藏非必要装饰）

**验收标准**：
- [ ] iPhone SE (375px) 到 Desktop (1920px+) 全覆盖
- [ ] 横竖屏切换布局正确适配
- [ ] 触控目标 ≥ 44px
- [ ] 词语在 2 米距离外清晰可读（投屏场景）

---

### 2.8 F7：游戏设置面板（P1）

**功能描述**：开局前的参数配置界面。

**设置项**：

| 设置项 | 类型 | 默认值 | 取值范围 | 说明 |
|--------|------|--------|----------|------|
| 玩家人数 | 数字选择器 | 6 | 4-12 | 步进为 1 |
| 卧底数量 | 数字选择器 | 1 | 1-2 | 受人数约束 |
| 白板数量 | 开关 + 数字 | 0 | 0-2 | 白板模式开启后可选 |
| 游戏模式 | Tab 切换 | 经典 | 经典/白板 | 切换模式重置部分默认值 |
| 词库难度 | Segmented Control | 随机 | 随机/简单/中等/困难 | 影响词对选取范围 |
| 描述轮次 | 数字选择器 | 1 | 1-3 | 每人描述几轮后再投票 |
| 计时器 | 开关 | 关 | 开/关 | 开启后每轮描述倒计时 |

**交互细节**：
- 参数变更实时预览角色分配预览（如"3 平民 vs 1 卧底 vs 2 白板"）
- 违反约束时显示内联错误提示（如"7人局最多1个卧底"）
- 支持"快速开始"按钮使用默认配置一键开局

**验收标准**：
- [ ] 所有设置项均可正常配置
- [ ] 约束校验实时生效
- [ ] 快速开始可用
- [ ] 设置面板 UI 清晰直观

---

### 2.9 F8：首次使用引导（P2）

**功能描述**：新用户首次打开时的 3 步引导。

**引导步骤**：

| 步骤 | 标题 | 内容 | 配图/动画 |
|------|------|------|-----------|
| 1 | 👋 欢迎使用 | "谁是卧底发牌助手，聚会神器！" | 产品 Logo 动画 |
| 2 | 📱 怎么玩 | "设置人数 → 发牌 → 传手机看词 → 开始游戏" | 流程示意图 |
| 3 | 🎮 开始吧 | "选择模式和人数，立即开玩！" | CTA 按钮 |

**实现要求**：
- 仅首次访问显示（localStorage 标记）
- 可随时从设置页重新查看引导
- 每步 1-2 屏幕内容，支持滑动切换
- 可跳过（右上角"跳过"按钮）

**验收标准**：
- [ ] 首次访问自动弹出引导
- [ ] 再次访问不再弹出
- [ ] 3 步内容完整
- [ ] 支持跳过和重新查看

---

## 三、非功能需求

### 3.1 性能需求

| 指标 | 目标值 | 测量方法 | 方案 |
|------|--------|----------|------|
| FCP（首屏绘制） | < 1.5s | Lighthouse | Vite 代码分割 + 预加载关键资源 |
| LCP（最大内容绘制） | < 2.0s | Lighthouse | 内联关键 CSS + 字体预加载 |
| TTI（可交互时间） | < 2.5s | Lighthouse | 懒加载非关键组件 |
| 翻牌动画帧率 | 稳定 60fps | DevTools Performance | Framer Motion + GPU 加速（transform/opacity） |
| 包体积（gzipped JS+CSS） | < 200KB | Build output | Tree-shaking + 按需引入 |
| 词库加载时间 | < 500ms | Network tab | 内置 JSON 直接 import；在线 API 带 cache |
| Lighthouse Performance | > 90 分 | Lighthouse Audit | 持续监控 |

### 3.2 兼容性需求

| 平台 | 最低版本 | 测试覆盖率 | 备注 |
|------|----------|------------|------|
| iOS Safari | 14+ | ✅ 必测 | 覆盖 iPhone 6s+ |
| Android Chrome | 90+ | ✅ 必测 | 覆盖 Android 8+ |
| 微信内置浏览器 | 最新版 | ✅ 必测 | 主要分享入口 |
| iPadOS Safari | 15+ | ⚠️ 应测 | 投屏场景 |
| 桌面 Chrome/Firefox/Safari | 最新版 | ⚠️ 应测 | 开发调试用 |

**不支持**：
- ❌ IE（任何版本）
- ❌ Android Webview 过旧版本（< Chrome 70）
- ❌ Opera Mini 等极端省流浏览器

### 3.3 可访问性（A11y）需求

| 需求 | 标准 | 实现方案 |
|------|------|----------|
| 键盘导航 | WCAG 2.1 AA | Tab 键可在卡片间切换，Enter/Space 翻牌 |
| 屏幕阅读器 | ARIA labels | 卡片状态（正面/背面）、角色信息有 aria-label |
| 颜色对比度 | WCAG AA（4.5:1） | 所有文本与背景对比度达标 |
| 减少动画 | `prefers-reduced-motion` | 自动禁用/简化 3D 翻转动画 |
| 字体缩放 | 支持 200% 缩放 | rem 单位 + 不固定 px 高度 |

### 3.4 安全需求

| 需求 | 方案 |
|------|------|
| XSS 防护 | 自定义词库输入消毒（DOMPurify），无 innerHTML 注入 |
| CSP 策略 | 严格的 Content-Security-Policy 头 |
| 无敏感数据 | 不收集个人信息，无 cookie（除必要偏好存储） |
| HTTPS 强制 | 生产环境全站 HTTPS（CF 自动提供） |
| 词库安全 | 在线词库服务端签名验证，防篡改 |

### 3.5 离线能力（v1.1）

| 能力 | 实现方案 |
|------|----------|
| 基础离线 | Service Worker 缓存静态资源 + 内置词库 |
| 离线提示 | 网络断开时显示"离线模式"badge |
| 离线功能 | 可使用内置词库正常发牌（在线词库不可用） |
| 同步策略 | 恢复网络后后台静默同步最新词库 |

---

## 四、用户故事与验收标准

### US-01：快速开始一局游戏

**作为** 聚会参与者，
**我想** 打开网页后 10 秒内开始一局游戏，
**以便** 不让朋友们等待太久。

**验收标准**：
- [ ] 首页有醒目的"快速开始"按钮
- [ ] 点击后使用默认配置（6人/1卧底/经典模式/随机难度）直接进入发牌
- [ ] 从首页到发牌完成 ≤ 3 次点击
- [ ] 首次访问用户也能快速开始（引导不阻塞）

### US-02：自定义游戏参数

**作为** 游戏组织者，
**我想** 自定义人数、卧底数、模式等参数，
**以便** 适应不同规模的聚会。

**验收标准**：
- [ ] 可设置 4-12 人
- [ ] 可设置 1-2 个卧底（受人数约束）
- [ ] 可切换经典/白板模式
- [ ] 可选择词库难度
- [ ] 参数非法时有明确提示

### US-03：安全地查看词语

**作为** 玩家，
**我想** 点击卡片翻转查看我的词语，
**以便** 只有我自己能看到，旁边的人看不到。

**验收标准**：
- [ ] 卡片默认背面朝上
- [ ] 点击后 3D 翻转到正面显示词语
- [ ] 可再次点击或点击"隐藏"翻回背面
- [ ] 翻转过程中正反面均不可见（中间帧遮盖）
- [ ] 词语字体足够大（≥24px），但不会溢出屏幕

### US-04：传递设备继续游戏

**作为** 聚会参与者，
**我想** 看完词后将手机传给下一个人，
**以便** 每个人都能安全地查看自己的词语。

**验收标准**：
- [ ] 查看完一张卡片后状态保持（不会自动重置）
- [ ] 已查看和未查看的卡片有视觉区分（如已查看的卡片略微透明或变色）
- [ ] 传递过程中误触不会导致词语泄露
- [ ] 界面底部有操作提示"请将设备传递给下一位玩家"

### US-05：了解游戏结果

**作为** 游戏参与者，
**我想** 游戏结束后查看所有人的身份和词语，
**以便** 回顾精彩瞬间。

**验收标准**：
- [ ] 结果页面展示所有玩家的：编号、角色、词语
- [ ] 明确标注哪一方获胜（平民胜/卧底胜/白板胜）
- [ ] 卧底的词语和平民的词语并列展示，方便对比
- [ ] 有"再来一局"按钮快速重开

### US-06：使用新鲜词库

**作为** 老玩家，
**我想** 每次玩游戏都能遇到新词对，
**以便** 不会因为词库重复而厌倦。

**验收标准**：
- [ ] 内置 300+ 词对，足够多次游戏不重复
- [ ] v1.1 支持在线拉取最新词库
- [ ] 显示当前词库版本/更新时间
- [ ] 可手动触发词库刷新

### US-07：在手机上流畅使用

**作为** 手机用户，
**我想** 在任何手机上都有流畅的使用体验，
**以便** 无论用什么设备都能随时开玩。

**验收标准**：
- [ ] iOS Safari 和 Android Chrome 均完美运行
- [ ] 微信内置浏览器正常运行
- [ ] 横屏和竖屏均有良好布局
- [ ] 单手可操作（主要操作在屏幕下半区）

---

## 五、信息架构与页面结构

### 5.1 页面路由

```
/                    → 首页（品牌展示 + 快速开始）
/setup               → 游戏设置页
/game                → 发牌/游戏页（翻牌交互）
/result              → 游戏结果页
/settings            → 全局设置（暗色模式、引导等）
```

> SPA 单页应用，路由切换使用客户端导航（React Router v6）。

### 5.2 首页（/）

**目的**：品牌展示 + 快速入口

**布局结构**：
```
┌──────────────────────────────┐
│          🔍 Logo + 标题       │
│     "谁是卧底 · 发牌助手"      │
│                              │
│    ┌────────────────────┐    │
│    │   🎮  快速开始       │    │    ← 主 CTA
│    │   6人局 · 经典模式    │    │
│    └────────────────────┘    │
│                              │
│    ┌────────┐ ┌────────┐    │
│    │ 自定义  │ │ 白板   │    │    ← 模式快捷入口
│    │ 设置 ▶  │ │ 模式 ▶ │    │
│    └────────┘ └────────┘    │
│                              │
│  ── 今日推荐词对 ──           │
│  🍎 苹果 vs 梨子              │    ← 展示趣味性
│                              │
│        Footer: GitHub ★ Star  │
└──────────────────────────────┘
```

### 5.3 游戏设置页（/setup）

**目的**：配置游戏参数

**布局结构**：
```
┌──────────────────────────────┐
│  ← 返回     游戏设置          │
│                              │
│  ── 模式选择 ──              │
│  [经典模式]  [白板模式]       │
│                              │
│  ── 玩家配置 ──              │
│  玩家人数  [ 6 ]  (-/+)      │
│  卧底数量  [ 1 ]  (-/+)      │
│  白板数量  [ 0 ]  (-/+)      │
│                              │
│  📊 预览: 4平民 vs 1卧底      │
│                              │
│  ── 词库设置 ──              │
│  难度: [随机|简单|中等|困难]  │
│                              │
│  ── 游戏规则 ──              │
│  描述轮次  [ 1 ]             │
│  ⏱️ 计时器  [关]             │
│                              │
│  ┌────────────────────┐    │
│  │   🎮 开始发牌！       │    │
│  └────────────────────┘    │
└──────────────────────────────┘
```

### 5.4 发牌游戏页（/game）

**目的**：核心交互——翻牌查看词语

**布局结构**：
```
┌──────────────────────────────┐
│  第 1/6 位玩家    [重新发牌]  │
│                              │
│                              │
│         ┌──────────┐         │
│         │          │         │
│         │   ? ? ?  │         │    ← 卡片背面
│         │          │         │
│         │  点击翻牌 │         │
│         └──────────┘         │
│                              │
│  ── 或 ──                    │
│         ┌──────────┐         │
│         │          │         │
│         │   苹 果   │         │    ← 卡片正面（翻转后）
│         │          │         │
│         │  📋 平民  │         │
│         │ [隐藏词语] │         │
│         └──────────┘         │
│                              │
│  💡 请查看后传递给下一位      │
│                              │
│  [⬅ 上一位]  [下一位 ➡]     │
└──────────────────────────────┘
```

**多卡片视图**（桌面/平板宽屏）：
```
┌──────────────────────────────┐
│  发牌结果 · 6人局             │
│                              │
│  ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ P1  │ │ P2  │ │ P3  │    │
│  │  ?  │ │  ?  │ │  ?  │    │
│  └─────┘ └─────┘ └─────┘    │
│  ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ P4  │ │ P5  │ │ P6  │    │
│  │  ?  │ │  ?  │ │  ?  │    │
│  └─────┘ └─────┘ └─────┘    │
│                              │
│  点击卡片翻转查看词语          │
└──────────────────────────────┘
```

### 5.5 游戏结果页（/result）

**目的**：展示游戏结局

**布局结构**：
```
┌──────────────────────────────┐
│                              │
│        🎉 平民获胜！          │
│                              │
│  ── 身份揭晓 ──              │
│                              │
│  P1 平民  🍎 苹果  ✅ 存活    │
│  P2 平民  🍎 苹果  ✅ 存活    │
│  P3 卧底  🕵️ 梨子  ❌ 淘汰    │
│  P4 平民  🍎 苹果  ✅ 存活    │
│  P5 卧底  🕵️ 梨子  ❌ 淘汰    │
│  P6 白板  📋 ——   ✅ 存活    │
│                              │
│  ┌────────────┐              │
│  │  🔄 再来一局  │              │
│  └────────────┘              │
│  [🏠 返回首页]                │
└──────────────────────────────┘
```

### 5.6 全局设置页（/settings）

**目的**：应用级别偏好设置

**设置项**：
- 暗色模式开关（跟随系统 / 明亮 / 暗色）
- 重新查看引导
- 关于/版本信息
- 反馈入口
- 词库信息（版本号、词对数量、最后更新时间）

---

## 六、技术约束

### 6.1 技术栈

| 层 | 技术 | 版本 | 用途 |
|----|------|------|------|
| 框架 | React | 18+ | UI 组件化开发 |
| 构建 | Vite | 5+ | 开发服务器 & 生产构建 |
| 样式 | TailwindCSS | 3+ | 原子化 CSS |
| 动画 | Framer Motion | 11+ | 翻牌/过渡动画 |
| 状态管理 | Zustand | 4+ | 轻量全局状态 |
| 路由 | React Router | 6+ | SPA 客户端路由 |
| 类型 | TypeScript | 5+ | 类型安全 |
| PWA | Workbox | 7+ | 离线缓存（v1.1） |
| 后端 | Cloudflare Workers | - | 词库 API + 定时更新 |
| 存储 | CF KV | - | 词库数据持久化 |
| 部署 | Cloudflare Pages | - | 静态托管 + 边缘部署 |

### 6.2 架构约束

1. **纯前端 SPA**：无 SSR，客户端渲染
2. **无用户系统**：无需认证/授权
3. **无数据库**（前端）：仅 localStorage + CF KV（后端）
4. **渐进增强**：核心功能不依赖动画和网络
5. **移动端优先**：响应式设计以移动端为第一优先级
6. **PWA 就绪**：v1.1 实现 Service Worker 离线

### 6.3 浏览器兼容性约束

- 必须支持 ES2020+ 语法（Vite 默认 @babel/preset-env 处理降级）
- 必须支持 CSS Grid + Flexbox
- 必须支持 CSS 3D Transforms（翻牌动画）
- 必须支持 Intersection Observer（懒加载）
- 必须支持 ResizeObserver（响应式卡片）

---

## 七、数据模型

### 7.1 词库数据模型

```typescript
// 词对
interface WordPair {
  id: string;              // 唯一标识 "w001"
  wordA: string;           // 平民词语
  wordB: string;           // 卧底词语
  difficulty: 'easy' | 'medium' | hard';
  tags: string[];          // 分类标签
  source?: string;         // 来源（builtin / online / custom / ai）
  createdAt?: string;      // 创建时间
}

// 词库集合
interface WordBank {
  version: string;           // 语义版本 "1.0.0"
  category: 'builtin' | 'online' | 'custom';
  words: WordPair[];
  meta: WordBankMeta;
}

interface WordBankMeta {
  total: number;
  difficultyDistribution: Record<string, number>;
  lastUpdated: string;       // ISO 8601
  checksum: string;          // SHA-256 完整性校验
}
```

### 7.2 游戏状态模型

```typescript
// 角色
type Role = 'civilian' | 'spy' | 'whiteboard';

// 玩家卡片
interface PlayerCard {
  playerId: number;          // 1-based
  role: Role;
  word: string | null;       // 白板为 null
  wordForDisplay?: string;   // 结果页展示用（掩码）
  isRevealed: boolean;       // 是否已翻牌查看
  isEliminated: boolean;     // 是否被淘汰
}

// 游戏配置
interface GameConfig {
  totalPlayers: number;      // 4-12
  spyCount: number;          // 1-2
  whiteboardCount: number;   // 0-2
  mode: 'classic' | 'whiteboard';
  difficulty: 'random' | 'easy' | 'medium' | 'hard';
  descriptionRounds: number; // 1-3
  timerEnabled: boolean;
  selectedWordPair?: WordPair; // 手动选择的词对
}

// 游戏状态（Zustand Store）
interface GameState {
  phase: 'idle' | 'dealing' | 'playing' | 'voting' | 'finished';
  config: GameConfig;
  cards: PlayerCard[];
  currentPlayerIndex: number; // 当前查看的玩家
  winner: 'civilian' | 'spy' | 'whiteboard' | null;
  eliminatedOrder: number[];  // 淘汰顺序
}
```

### 7.3 用户偏好模型

```typescript
interface UserPreferences {
  theme: 'light' | 'dark' | 'system';
  seenOnboarding: boolean;
  language: 'zh-CN';          // MVP 仅中文
  lastGameConfig?: GameConfig; // 上次配置，方便快速重开
}
```

### 7.4 API 接口定义

#### GET /api/words

获取在线词库。

**请求**：
```
GET /api/words?difficulty=random&limit=50&since=2026-04-20
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| difficulty | string | 否 | 筛选难度：random/easy/medium/hard |
| limit | number | 否 | 返回数量限制，默认 50，最大 200 |
| since | string | 否 | 增量更新，返回该日期后的新词对 |

**响应（200 OK）**：
```json
{
  "success": true,
  "data": {
    "version": "1.2.0",
    "words": [
      {
        "id": "w301",
        "wordA": "奶茶",
        "wordB": "咖啡",
        "difficulty": "easy",
        "tags": ["饮品", "生活"],
        "source": "online"
      }
    ],
    "meta": {
      "total": 450,
      "difficultyDistribution": { "easy": 150, "medium": 150, "hard": 150 },
      "lastUpdated": "2026-04-25T06:00:00Z",
      "checksum": "abc123..."
    }
  }
}
```

**错误响应**：
| 状态码 | 场景 | 处理 |
|--------|------|------|
| 429 | 请求过频 | 客户端退避重试（指数退避） |
| 500 | 服务端错误 | 降级到内置词库 |
| 503 | 维护中 | 显示"词库暂不可用，使用本地词库" |

#### GET /api/words/info

获取词库元信息（轻量接口，用于检查更新）。

**响应**：
```json
{
  "success": true,
  "data": {
    "version": "1.2.0",
    "totalWords": 450,
    "lastUpdated": "2026-04-25T06:00:00Z",
    "checksum": "abc123..."
  }
}
```

#### POST /api/words/submit（v2.0）

用户提交词对（社区词库功能预留）。

**请求体**：
```json
{
  "wordA": "示例词A",
  "wordB": "示例词B",
  "difficulty": "medium",
  "submitterNote": "来自社区"
}
```

### 7.5 本地存储 Schema

```typescript
// localStorage keys
const STORAGE_KEYS = {
  PREFS: 'spy_prefs',              // UserPreferences JSON
  GAME_HISTORY: 'spy_history',      // 游戏记录数组
  CUSTOM_WORDS: 'spy_custom_words', // 自定义词库
  USED_WORD_IDS: 'spy_used_ids',    // 本次 session 已用词 ID
  WORD_CACHE: 'spy_word_cache',     // 在线词库缓存 + 缓存时间
  ONBOARDING_SEEN: 'spy_onboarding', // 'true'
} as const;
```

---

## 八、MVP 范围与迭代计划

### 8.1 MVP 范围（v1.0）

**包含功能**：
- ✅ F1 发牌核心引擎
- ✅ F2 经典游戏模式
- ✅ F3 白板游戏模式
- ✅ F4 内置词库（300+ 词对）
- ✅ F5 翻牌动画（3D 翻转）
- ✅ F6 响应式布局（移动端优先）
- ✅ F7 游戏设置面板
- ⚠️ F8 首次使用引导（若时间允许）

**不包含**（明确延后）：
- ❌ 在线词库 API（v1.1）
- ❌ 自定义词库（v1.1）
- ❌ PWA 离线（v1.1）
- ❌ 暗色模式（v1.1）
- ❌ AI 生成词对（v2.0）
- ❌ 社区词库（v2.0）

### 8.2 迭代计划

#### Sprint 1（Week 1-2）：核心功能

| 任务 | 估时 | 产出 |
|------|------|------|
| 项目初始化（Vite + React + TS + Tailwind） | 0.5d | 可运行骨架 |
| TypeScript 类型定义 | 0.5d | types/game.ts |
| 发牌核心逻辑（分配算法） | 1d | utils/dealer.ts |
| Zustand 状态管理 | 0.5d | stores/gameStore.ts |
| 内置词库数据文件（300+ 词对） | 1d | data/words.json |
| 词库加载与筛选逻辑 | 0.5d | services/wordBank.ts |
| CardDealer 组件 | 1d | components/game/CardDealer.tsx |
| WordCard 翻牌组件 | 1d | components/game/WordCard.tsx |
| PlayerSetup 设置组件 | 1d | components/game/PlayerSetup.tsx |
| GameResult 结果组件 | 0.5d | components/game/GameResult.tsx |
| **小计** | **~8d** | |

#### Sprint 2（Week 3）：交互与动画

| 任务 | 估时 | 产出 |
|------|------|------|
| Framer Motion 翻牌动画集成 | 1d | 60fps 3D 翻转 |
| 响应式布局实现 | 1.5d | 5 断点全覆盖 |
| 路由配置（React Router v6） | 0.5d | 5 个页面 |
| 首页 UI | 1d | 品牌 + 快速开始 |
| 设置页 UI | 1d | 完整设置表单 |
| 游戏流程串联 | 1d | 设置→发牌→结果 完整链路 |
| **小计** | **~6d** | |

#### Sprint 3（Week 4）：测试与发布

| 任务 | 估时 | 产出 |
|------|------|------|
| 首次使用引导 | 0.5d | 3 步引导 |
| 边界情况处理 | 1d | 异常输入/极端配置 |
| 多浏览器测试 | 1d | iOS/Android/微信 |
| 性能优化 | 1d | Lighthouse > 90 |
| CF Pages 部署配置 | 0.5d | 生产环境上线 |
| 域名 + DNS 配置 | 0.5d | 自定义域名 |
| **小计** | **~4.5d** | |

**MVP 总工期**：约 **18.5 天**（约 3.5 周，1 人全职）

### 8.3 v1.1 计划（MVP 后 2-3 周）

| 周次 | 重点 | 交付 |
|------|------|------|
| Week 5-6 | CF Workers 词库 API + Cron 爬取 | 在线词库可用 |
| Week 6 | PWA（Service Worker + Manifest） | 可安装到主屏幕 |
| Week 6-7 | 自定义词库 + 暗色模式 | 用户体验增强 |
| Week 7 | 集成测试 → 发布 v1.1 | v1.1 上线 |

### 8.4 v2.0 远期规划（MVP 后 2-3 月）

| 模块 | 内容 | 依赖 |
|------|------|------|
| AI 词对生成 | LLM API 批量生成 + 审核 | 付费 API key |
| 社区词库 | 用户提交 + 投票系统 | 用户体系基础 |
| 数据统计 | 使用分析面板 | 埋点系统 |
| 多语言 | 英文/日文词库包 | i18n 方案 |

---

## 九、风险与依赖

### 9.1 风险登记册

| ID | 风险 | 概率 | 影响 | 等级 | 缓解措施 |
|----|------|------|------|------|----------|
| R01 | 词库版权争议 | 低 | 中 | 🟡 | 只收录通用词汇；建立审核机制；开源协议词库优先 |
| R02 | CF Workers 免费额度不足 | 极低 | 低 | 🟢 | 免费额度 10 万次/日 >> 实际 ~1000 次/日；$5/月可升级 |
| R03 | 爬虫反制导致词库更新失败 | 中 | 中 | 🟡 | 多源采集（GitHub 为主）；本地缓存降级；手动更新兜底 |
| R04 | 移动端浏览器兼容性问题 | 低 | 低 | 🟢 | Framer Motion 自动降级；核心功能不依赖动画；充分测试 |
| R05 | 微信内置浏览器限制 | 中 | 中 | 🟡 | 提前测试微信 WebView；规避已知限制（如下载弹窗）；备选浏览器引导 |
| R06 | 初始词库质量不足 | 中 | 高 | 🟠 | 启动前投入 2 天精选 300+ 词对；参考多个开源词库交叉验证 |
| R07 | 用户增长慢于预期 | 中 | 中 | 🟡 | 小红书/抖音传播；GitHub 开源获客；产品自身口碑 |
| R08 | SEO 差导致自然流量低 | 中 | 低 | 🟢 | 主要靠社交传播非 SEO；后续可加预渲染改善 |

### 9.2 依赖清单

| 依赖 | 类型 | 描述 | 获取方式 |
|------|------|------|----------|
| Cloudflare 账户 | 外部 | Workers + KV + Pages 托管 | 免费注册 |
| 域名 | 外部 | 自定义域名（可选） | 已有或购买 ~¥60/年 |
| 初始词库数据 | 内容 | 300+ 高质量词对 | 从开源仓库收集 + 人工审核 |
| GitHub 开源词库 | 外部 | 词库爬取来源 | MIT 协议仓库 |
| 设计资源 | 内部 | Logo + 图标 + 配色方案 | 可用 AI 生成 + Iconify 图标库 |

### 9.3 决策记录（ADR）

| # | 决策 | 背景 | 结论 |
|---|------|------|------|
| ADR-001 | 选择 React 而非 Vue | 团队 React 经验更丰富；生态更大 | React 18 + TypeScript |
| ADR-002 | 选择 Vite 而非 Webpack | 开发体验好；HMR 极快；打包体积小 | Vite 5 |
| ADR-003 | 选择 Zustand 而非 Redux | 应用状态简单；Zustand API 更简洁；bundle 更小 | Zustand 4 |
| ADR-004 | 选择 CF Workers 而自建服务器 | 免费额度够用；零运维；全球 CDN | CF Workers + KV |
| ADR-005 | MVP 不做用户系统 | 聚会场景不需要；降低复杂度；加快开发 | 无认证 |
| ADR-006 | MVP 不做在线词库 | 先验证核心功能；CF Workers 作为 v1.1 交付 | 内置词库起步 |

---

## 附录

### A. 词库样例（前 20 对）

| # | 词A（平民） | 词B（卧底） | 难度 | 标签 |
|---|-----------|-----------|------|------|
| 1 | 苹果 | 梨子 | easy | 水果 |
| 2 | 红烧肉 | 糖醋排骨 | easy | 食物 |
| 3 | 小狗 | 小猫 | easy | 动物 |
| 4 | 手机 | 平板 | easy | 数码 |
| 5 | 篮球 | 足球 | easy | 运动 |
| 6 | 奶茶 | 咖啡 | easy | 饮品 |
| 7 | 孙悟空 | 哪吒 | medium | 神话 |
| 8 | 刘备 | 曹操 | medium | 三国 |
| 9 | 雪花 | 冰雹 | medium | 自然 |
| 10 | 奥特曼 | 假面骑士 | medium | 特摄 |
| 11 | 心跳 | 脉搏 | medium | 医学 |
| 12 | 初恋 | 暗恋 | medium | 情感 |
| 13 | 量子计算 | 区块链 | hard | 科技 |
| 14 | 存在主义 | 虚无主义 | hard | 哲学 |
| 15 | 莫扎特 | 贝多芬 | hard | 音乐 |
| 16 | 释迦牟尼 | 耶稣 | hard | 宗教 |
| 17 | RNA | DNA | hard | 生物 |
| 18 | 量子纠缠 | 引力波 | hard | 物理 |
| 19 | 后现代主义 | 结构主义 | hard | 艺术 |
| 20 | 形而上 | 形而下 | hard | 哲学 |

> 完整 300+ 词对见 `data/words.json` 文件。

### B. 设计规范摘要

| 维度 | 规范 |
|------|------|
| **主色调** | `#6C5CE7`（紫罗兰）— 神秘感 + 聚会氛围 |
| **辅助色** | `#00B894`（翠绿）— 成功/平民；`#E17055`（橙红）— 卧底/警告 |
| **背景色** | Light: `#F8F9FA`; Dark: `#1A1A2E` |
| **字体** | 中文：系统默认（-apple-system / PingFang SC）；英文/数字：Inter |
| **圆角** | 卡片 `16px`，按钮 `12px`，输入框 `8px` |
| **阴影** | 卡片 `0 4px 20px rgba(0,0,0,0.08)` |
| **动画** | 翻转 600ms easeInOut；页面切换 300ms |
| **图标** | Iconify（lucide 图标集） |

### C. 术语表

| 术语 | 定义 |
|------|------|
| 平民 | 拿到相同词语 A 的普通玩家 |
| 卧底 | 拿到相似词语 B 的隐藏角色 |
| 白板 | 无词语的角色，需要伪装混入 |
| 经典模式 | 标准规则：平民 + 卧底 |
| 白板模式 | 增加白板角色的增强模式 |
| 发牌 | 分配角色和词语的过程 |
| 翻牌 | 卡片 3D 翻转查看词语的交互 |
| 同屏面对面 | 一台设备多人传递的使用模式 |
| 词对 | 平民词 A + 卧底词 B 的配对 |
| CF Workers | Cloudflare Workers 无服务器计算平台 |
| PWA | Progressive Web App，可安装的 Web 应用 |

---

*文档编写：PM Agent | 基于调研报告 v2.0 | 完成时间：2026-04-25 22:20 CST*
*下一步：Gate 1 审批 → UI 原型设计*
