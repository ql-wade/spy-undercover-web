# 谁是卧底 · 交互原型设计说明

> 项目：spy-undercover | 版本：v1.0 | 日期：2026-04-25
> 产出：prototype.html（可交互 HTML 原型）

---

## 一、设计概览

### 1.1 设计理念

**"悬疑之夜，指尖揭晓"** — 以神秘/悬疑为视觉基调，深色主题营造聚会氛围。核心交互围绕"传手机"这一线下场景设计，每一步都为防偷看和流畅传递优化。

### 1.2 视觉系统

| 维度 | 规范 | 说明 |
|------|------|------|
| **色系** | 紫罗兰色调暗色主题 | 主色 #7C6AEF，所有灰色添加紫色偏移 |
| **背景** | #0B0A14（最深）→ #1A1832（表面） | 不是纯黑，带有紫色调的深色 |
| **文字** | #E8E5F5 主文字，#908BA8 辅助 | 避免纯白，柔和不刺眼 |
| **角色色** | 绿 #2DD4A0（平民）/ 红 #E8534A（卧底）/ 琥珀 #E8A830（白板） | 语义明确，高对比度 |
| **圆角** | 8px / 12px / 16px | 不超过 16px，避免过度圆润 |
| **字体** | Manrope + 系统中文字体 | 不使用 Inter/Roboto |
| **阴影** | 低强度，带品牌色调 | `rgba(124,106,239,0.2)` 紫色辉光 |
| **毛玻璃** | `backdrop-filter: blur(16px)` | 卡片层级感，不过度使用 |

### 1.3 设计原则

1. **任务优先**：每个屏幕一个主要行动，3 秒内理解当前该做什么
2. **防偷看**：词语始终有遮罩，需主动操作才短暂显示
3. **传递引导**：明确的"当前是谁"提示和"传给下一位"引导
4. **戏剧性**：揭晓环节有逐个动画、角色色彩对比，制造紧张感
5. **聚会友好**：大按钮（最小 44px 触控区）、大字体（词语 2.5rem）

---

## 二、页面设计详解

### 2.1 首页（Home）

**目的**：品牌展示 + 快速入口

**页面元素**：
| 元素 | 说明 |
|------|------|
| Logo 图标 | 72px 紫色渐变圆角方块，内含扑克牌符号 |
| 标题 | "谁是卧底" — 2rem/800 粗体 |
| 副标题 | "聚会发牌助手" — 辅助文字色 |
| 快速开始按钮 | 主 CTA，全宽紫色按钮，带辉光阴影 |
| 自定义设置 | 次级入口，左列 |
| 白板模式 | 次级入口，右列 |
| 今日词对 | 随机展示一个词对（绿 VS 红），增加趣味 |
| 词库信息 | 底部链接 |

**操作流程**：
```
首页 → 快速开始 → 直接 6 人经典局 → 发牌页
首页 → 自定义设置 → 设置页
首页 → 白板模式 → 设置页（预选白板模式）
首页 → 词库信息 → 词库页
```

**状态转换**：
- `#screen-home` → `#screen-deal`（快速开始）
- `#screen-home` → `#screen-setup`（自定义/白板）
- `#screen-home` → `#screen-bank`（词库）

**动效**：
- 页面进入：fadeIn（300ms，translateY 8px → 0）
- Logo 呼吸动画（微弱 pulse）
- 词对每次刷新随机

**边界情况**：
- 无网络：页面纯静态，不依赖网络
- 首次访问：显示 splash 过渡（1.2s 后自动消失）

---

### 2.2 设置页（Setup）

**目的**：配置游戏参数

**页面元素**：
| 元素 | 类型 | 默认值 | 约束 |
|------|------|--------|------|
| 返回按钮 | 返回首页 | — | — |
| 模式切换 | Segmented Control | 经典 | 经典/白板 |
| 玩家人数 | Stepper | 6 | 4-10，步进 1 |
| 卧底数量 | Stepper | 1 | 1-2；≤5 人时最大 1 |
| 白板数量 | Stepper | 1（白板模式） | 0-2；仅白板模式显示 |
| 词库难度 | Segmented Control | 随机 | 随机/简单/中等/困难 |
| 描述轮次 | Stepper | 1 | 1-3 |
| 发言计时器 | Toggle | 关 | 开/关 |
| 角色预览 | Badge 行 | — | 实时计算 |
| 开始发牌 | 主 CTA | — | 校验通过后可用 |

**操作流程**：
```
设置页 → 调整参数 → 实时预览更新 → 点击"开始发牌" → 发牌页
设置页 → 点击返回 → 首页
```

**约束校验逻辑**：
1. 平民数 = 总人数 - 卧底数 - 白板数，必须 ≥ 1
2. 平民数必须 > 卧底数
3. ≤ 5 人时，最多 1 个卧底
4. 白板数上限 = floor((总人数 - 卧底数 - 1) / 2)
5. 违反约束时自动修正值（clamp），不弹错误

**状态转换**：
- `#screen-setup` → `#screen-deal`（开始发牌）
- `#screen-setup` → `#screen-home`（返回）

**动效**：
- Stepper 按钮点击：scale(0.92) 反馈
- Toggle 切换：滑块 200ms 位移
- 预览 Badge 实时更新
- Segmented Control 切换：背景色滑移

**边界情况**：
- 极端配置（4 人 2 卧底）：自动限制，无法选择
- 白板模式关闭后白板数量重置为 0

---

### 2.3 发牌页（Dealing）

**目的**：核心发牌交互 — 轮流翻牌查看词语

**页面元素**：
| 元素 | 说明 |
|------|------|
| 重新发牌按钮 | 左上角，弹确认框 |
| 玩家指示器 | "玩家 1 / 6" 右对齐 |
| 进度点阵 | 每个玩家一个点，当前紫色、已查看灰色、未查看暗色 |
| 翻牌卡片 | 中央大卡片（280×380px），支持 3D 翻转 |
| 卡片背面 | 紫色渐变 + 点状纹理 + 问号 + "点击翻牌查看" |
| 卡片正面 | 角色标签 + 大字词语 + 隐藏按钮 |
| 传递提示 | 底部文字提示当前操作 |
| 上一位/下一位 | 底部导航按钮 |

**防偷看机制**：
1. 卡片默认背面朝上，词语完全不可见
2. 点击卡片 → 600ms 3D 翻转动画（Y 轴旋转 180°）
3. 翻转过程中，中间帧由 `backface-visibility: hidden` 确保正反面不透出
4. 看完词语后需主动点击"隐藏词语"按钮翻回
5. 卡片正面不自动隐藏，必须手动操作
6. 下一位/上一位切换时强制翻回背面

**操作流程**：
```
进入发牌页 → 看到"玩家 1"和背面卡片
  → 点击卡片 → 3D 翻转 → 看到角色和词语
  → 点击"隐藏词语" → 翻回背面
  → 提示"请将手机传给下一位"
  → 点击"下一位" → 切换到玩家 2
  → ... 重复 ...
  → 最后一位点击"所有人已看完，开始游戏" → 游戏进行页
```

**状态转换**：
- `#screen-deal` → `#screen-play`（全部看完）
- `#screen-deal` → `#screen-deal`（重新发牌，内部刷新）

**动效**：
- 翻牌：`transform: rotateY(180deg)` 600ms cubic-bezier(0.4, 0, 0.2, 1)
- 翻回：反向动画
- 进度点：当前点变宽（8px → 24px），已查看变色
- 重新发牌：确认弹窗

**边界情况**：
- 误触翻牌：正面显示时不会重复翻转，只能通过"隐藏"翻回
- 中途切换玩家：自动翻回当前卡片
- 重新发牌：弹确认框，确认后重新生成角色和词语

---

### 2.4 游戏进行页（Play）

**目的**：描述阶段管理 — 提示当前发言者

**页面元素**：
| 元素 | 说明 |
|------|------|
| 轮次标识 | 左上角紫色 Badge "第 N 轮" |
| 计时器 | 右上角（可选），30s 倒计时，≤10s 变黄，≤5s 变红闪烁 |
| 发言者卡片 | 中央大数字 + "号玩家发言" |
| 描述提示 | "请描述你的词语，不能直接说出词语本身" |
| 描述顺序 | 底部 "1 → 2 → 3 → ..." |
| 下一位发言 | 次级按钮 |
| 开始投票 | 主 CTA（最后一位发言者时显示） |

**操作流程**：
```
进入游戏页 → 显示 1 号玩家发言
  → 玩家 1 描述完成 → 点击"下一位发言"
  → 显示 2 号玩家 → ... 重复 ...
  → 最后一位 → "开始投票"按钮出现
  → 点击"开始投票" → 投票页
```

**计时器行为**（开启时）：
- 每位发言者 30 秒倒计时
- ≤ 10 秒：文字变黄
- ≤ 5 秒：文字变红 + 脉冲动画
- 0 秒：Toast 提示"发言时间到！"
- 切换下一位时重置为 30 秒

**状态转换**：
- `#screen-play` → `#screen-vote`（开始投票）
- `#screen-play` → `#screen-play`（下一位发言者）

**动效**：
- 页面切换：fadeIn
- 发言者数字：直接更新（无动画，保持简洁）
- 计时器：每秒更新，数字 tabular-nums 等宽

**边界情况**：
- 所有玩家被淘汰（极端情况）：应在投票结果中判定
- 计时器到 0 不自动跳转：由玩家手动操作

---

### 2.5 投票页（Voting）

**目的**：选择淘汰对象

**页面元素**：
| 元素 | 说明 |
|------|------|
| 标题 | "投票淘汰" |
| 副标题 | "选择你认为的卧底" |
| 玩家网格 | 3 列网格，每个存活玩家一张卡片 |
| 投票卡片 | 玩家编号 + "玩家 N" 标签 |
| 确认投票 | 底部主按钮，选中后可用 |

**投票公平性设计**：
- 投票是单人操作（传手机模式）
- 每次只投一票
- 原型中为演示目的，模拟其他玩家投票（随机）
- 生产环境中应改为轮流投票流程

**操作流程**：
```
进入投票页 → 看到所有存活玩家
  → 点击一个玩家卡片 → 卡片高亮（红色边框）
  → 点击"确认投票" → 计算所有票数 → 投票结果页
```

**状态转换**：
- `#screen-vote` → `#screen-vote-result`（确认投票后）

**动效**：
- 选中卡片：红色边框 + 红色背景高亮（200ms）
- 按钮从 disabled 到 enabled 状态

**边界情况**：
- 未选择就点击确认：按钮 disabled，无法提交
- 已淘汰玩家：灰色、不可点击

---

### 2.6 投票结果页（Vote Result）

**目的**：展示投票统计和淘汰结果

**页面元素**：
| 元素 | 说明 |
|------|------|
| 标题 | "投票结果" |
| 票数条形图 | 每行：玩家编号 + 进度条 + 票数 |
| 淘汰高亮 | 得票最多者红色背景 |
| 继续游戏 | 主按钮（游戏未结束时） |
| 查看结果 | 主按钮（游戏已结束时，跳转揭晓页） |
| 直接揭晓 | 次级按钮（调试用，生产环境移除） |

**胜负判定逻辑**：
```
投票淘汰一人后检查：
  卧底全部淘汰 → 平民获胜 → 显示"查看结果"
  平民数 ≤ 卧底数 → 卧底获胜 → 显示"查看结果"
  否则 → 继续游戏 → 回到游戏进行页（下一轮）
```

**操作流程**：
```
投票结果页 → 查看票数分布 → 自动判定胜负
  → 游戏继续 → "继续游戏" → 下一轮描述
  → 游戏结束 → "查看结果" → 揭晓页
```

**动效**：
- 进度条：600ms 宽度过渡动画
- 淘汰行：红色背景 + 红色边框

---

### 2.7 揭晓页（Reveal）

**目的**：戏剧性揭示所有身份和词语

**页面元素**：
| 元素 | 说明 |
|------|------|
| 胜方标题 | "平民获胜" / "卧底获胜"，对应颜色 |
| 词对信息 | "平民词：苹果 | 卧底词：梨子" |
| 身份卡片 | 每个玩家一张：头像（编号）+ 角色标签 + 词语 + 状态 |
| 再来一局 | 主按钮 |
| 返回首页 | 次级按钮 |

**揭晓动画**：
1. 所有卡片初始 `opacity: 0, translateX: -20px`
2. **卧底卡片优先显示**（制造紧张感）
3. 逐个滑入：每张间隔 250ms
4. 卧底卡片红色背景 + 红色边框
5. 白板卡片琥珀色背景
6. 已淘汰卡片降低透明度

**操作流程**：
```
进入揭晓页 → 逐个看到玩家身份
  → 卧底先揭晓（红色高亮）
  → 所有人揭晓完毕
  → 点击"再来一局" → 重新发牌
  → 点击"返回首页" → 首页
```

**状态转换**：
- `#screen-reveal` → `#screen-deal`（再来一局）
- `#screen-reveal` → `#screen-home`（返回首页）

**动效**：
- 卡片滑入：`opacity 0→1, translateX(-20px)→0` 400ms
- 每张间隔 250ms
- 卧底优先出现（先显示最关键信息）

**边界情况**：
- 快速离开：动画未完成也能操作
- 窗口失焦：动画不依赖焦点

---

### 2.8 词库管理页（Word Bank）

**目的**：展示词库信息和更新状态

**页面元素**：
| 元素 | 说明 |
|------|------|
| 返回按钮 | 返回首页 |
| 词对总数 | 大数字展示 |
| 词库版本 | 版本号 |
| 难度分布 | 简单/中等/困难各多少词对 |
| 最后更新 | 日期 |
| 刷新按钮 | 触发在线词库更新（v1.1 功能） |
| 提示文字 | "v1.1 版本将支持在线词库自动更新" |

---

## 三、交互状态机

```
                     ┌─────────────┐
                     │    首页     │
                     └──────┬──────┘
                      ┌─────┼──────┐
                 快速开始  自定义  词库
                      │     │      │
                      ▼     ▼      ▼
              ┌────────────────┐ ┌────────┐
              │    发牌页      │ │ 词库页 │
              └────────┬───────┘ └────────┘
                       │ 全部看完
                       ▼
              ┌────────────────┐
              │   游戏进行页    │◄──────┐
              └────────┬───────┘       │
                  最后一人│          继续游戏
                       ▼              │
              ┌────────────────┐      │
              │    投票页      │      │
              └────────┬───────┘      │
                  确认投票│            │
                       ▼              │
              ┌────────────────┐      │
              │   投票结果页    │──────┘
              └────────┬───────┘
                  游戏结束│
                       ▼
              ┌────────────────┐
              │    揭晓页      │
              └────────┬───────┘
                   再来一局│ 返回首页
                       ▼
              ┌────────────────┐
              │    发牌页      │ 或 首页
              └────────────────┘
```

---

## 四、响应式策略

| 断点 | 宽度 | 调整 |
|------|------|------|
| xs | 320-375px | 卡片宽度 80vw，保持可翻 |
| sm | 376-430px | 标准设计尺寸 |
| md | 431-768px | 投票网格 4 列 |
| lg | 769px+ | 居中 max-width: 430px，两侧留白 |

原型以 `max-width: 430px` 居中，确保在所有设备上保持一致的移动端体验。

---

## 五、无障碍设计

| 需求 | 实现 |
|------|------|
| 颜色对比度 | 文字与背景对比度 ≥ 4.5:1（WCAG AA） |
| 触控目标 | 所有按钮 ≥ 44×44px |
| 减少动画 | 预留 `prefers-reduced-motion` 媒体查询接口 |
| 语义 HTML | 使用 header/main/button 等语义标签 |
| 焦点可见 | 按钮有 focus-visible 样式 |

---

## 六、预留设计（v1.1+）

| 功能 | 预留接口 | 说明 |
|------|----------|------|
| 在线词库 | 词库页"刷新在线词库"按钮 | v1.1 对接 CF Workers API |
| PWA 离线 | 不涉及 UI | Service Worker 层 |
| 自定义词库 | 设置页可扩展"自定义词库"入口 | v1.1 |
| 暗色模式 | 当前默认暗色，v1.1 增加亮色切换 | — |
| 音效提示 | 揭晓页预留音效位置 | `// TODO: play sound` |

---

*设计完成时间：2026-04-25*
*原型文件：prototype.html — 浏览器直接打开即可体验完整流程*
